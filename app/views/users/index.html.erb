<h3>Hello This User Page</h3>

<%= form_for @user do |u| %>
	<%= u.text_field :first_name %>
	<%= u.text_field :last_name %>
	<%= u.text_field :email %>
	<%= u.text_field :password %>
	<%= u.submit 'Create' %>
	<% end %>